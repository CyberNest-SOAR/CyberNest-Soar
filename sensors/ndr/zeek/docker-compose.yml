services:
  zeek:
    build: .
    container_name: zeek
    network_mode: host
    privileged: true
    volumes:
      - ./logs:/opt/zeek/logs/current
      - ./zeek:/opt/zeek/share/zeek/site
    restart: unless-stopped
