services:
  velociraptor:
    image: graype28/velociraptor:latest
    container_name: velociraptor
    ports:
      - "8000:8000"
      - "8889:8889"
    volumes:
      - ./velociraptor-data:/data
    command:
      - server
      - --config
      - /data/config.yaml